module PiTin where

import qualified PiAST as P
import qualified Tin as T

{-
stmtToProc :: T.Stmt -> P.Proc
stmtToProc (Sexp e) = undefined
stmtToProc (SReceive vs) = undefined
stmtToProc (SSend Name es) = undefined
stmtToProc (SWhile e s) = undefined
-}
